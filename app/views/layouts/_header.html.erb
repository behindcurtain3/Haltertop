<header>
  <nav>
    <ul>
      <li><%= link_to image_tag("logo.png"), root_path %></li>
  <% unless signed_in? %>
      <%= render 'shared/guest_menu' %>
  <% else %>
      <%= render 'shared/user_menu' %>
  <% end %>
    </ul>
  </nav>
</header>