<div class="sidebar">
<% if signed_in? %>
  <%= render 'shared/user_bar' %>
<% else %>
  <%= render 'shared/quick_signup' %>
<% end %>
</div>
<div>
  <h3>Getting Started</h3>
  <p>SocialChess is a multiplayer <span class="red">chess app</span> currently in alpha testing.</p>
  <% if signed_in? %>
  <%= link_to "Play Now", play_path, :class => "button orange" %>
  <% end %>
</div>