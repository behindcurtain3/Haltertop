<header>
  <div class="container">
    <%= link_to image_tag("logo.png"), root_path, :class => "left" %>
    <nav>
      <ul>
    <% unless signed_in? %>
        <%= render 'shared/signin' %>
    <% else %>
        <%= render 'shared/user_menu' %>
    <% end %>
      </ul>
    </nav>
  </div>
</header>